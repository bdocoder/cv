{
  "name": "cv",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "postbuild": "npm run build:cv",
    "preview": "vite preview",
    "build:cv": "node scripts/build-cv.js",
    "postinstall": "npx playwright install chromium"
  },
  "devDependencies": {
    "playwright": "^1.46.0",
    "sass": "^1.71.1",
    "vite": "^5.1.4"
  },
  "dependencies": {
    "bulma": "^1.0.0"
  }
}
